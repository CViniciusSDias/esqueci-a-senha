import { SenhaPipePipe } from './senha-pipe.pipe';

describe('SenhaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SenhaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
